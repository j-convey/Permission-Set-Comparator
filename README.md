# Permission-Set-Comparator
Desktop utility to compare Salesforce permission sets between two users and list permission sets that the mirror user has but the primary user lacks, with descriptions sourced from a CSV.
